# Copy to .env.local. Both are optional — src/lib/env.ts already defaults the
# URL to the eu-west-2 project. The publishable (anon) key is safe client-side;
# RLS is the security boundary (spec 11 §2). Never put a service key here.
VITE_SUPABASE_URL=https://zyvhcnhablkgbsgtljma.supabase.co
VITE_SUPABASE_ANON_KEY=
