import { EmptyState } from '../components'

/** CSV import wizard — spec 06 §5. Replaced by features/import (P1). */
export function ImportRoute() {
  return (
    <EmptyState
      title="Import contacts"
      hint="The spreadsheet import wizard lands here (spec 06 §5)."
    />
  )
}
