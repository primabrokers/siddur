Additional browser QA of initial integrated UI at390×844 (viewport reset after testing):
- Mobile tab strip appears (Klaf / Measure / Shemos / Checks / Bench), but clicking Measure only changes focus; tikkun panel stays visible and calibration controls do not appear in the screenshot. Verify actual tab-panel switching, not just button presence.
- Main source/profile/geometry selectors and Compute layout button disappear from visible phone header. Ensure the complete create/import/calibrate/compute workflow is accessible on phone without desktop-only controls.
- Bottom bench tabs retain white backgrounds/nearly-white text and wrap into cramped rows.

Include these cases in DeepSeek UI corrections and Opus/browser acceptance. No product edits by controller. Corrector already has desktop clipping/units mismatch report.
