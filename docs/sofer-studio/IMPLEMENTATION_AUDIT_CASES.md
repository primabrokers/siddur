Controller observations on the first implementation wave; recheck after children finish because files are still being written. These are concrete acceptance cases for DeepSeek corrections and independent Opus review, not a final verdict.

1. engine/layout.js applyStretch: duplicate decisions for one occurrence each below cap can sum above cap; applying the SAME decision twice subtracts from already-reduced leftover. Reject duplicates or aggregate per occurrence before cap checking; always recompute from immutable BASE width. Repeated replacement of a decision set must be idempotent. Manual requests must obey active position filters too. Unknown/Shem/overcap requests should fail atomically, not silently return partial success.
2. computeLayout currently never calls resolvePatternsForSource or renderPattern. Merely exporting these functions is not functional pattern integration. With no patternDefs resolvePatterns returns zero blockers; known source ranges must not silently wrap. Fixed renderPattern currently has words:[], shem:false, drops positions/segments/gaps and adds a trailing word gap. Preserve real source tokens/occurrence IDs/Shem flags and slot segment geometry; ensure exact source coverage (no omissions, duplicates or substitutions). Reject unsupported data clearly.
3. buildWordUnits skips every inline marker then emits only verse.after at the end. For `אב {ס} גד` or `אב {פ} גד`, the boundary must remain BETWEEN those words, not move to the end of the verse. setuma at the start currently pushes a gap with currentWidth=0; test gap is measured exactly once and not supplemented by unintended word gaps.
4. Unusual-letter mapping: processSource drops occurrence_index; resolveOverrides resets letter counts for EACH WORD and defaults to matching all occurrences of same base. One selected occurrence must affect ONLY that occurrence, using stable source occurrence ID; repeated same letters in multiple words of one verse are a necessary test. U+05C4/U+05C5 unusual upper/lower dots are currently stripped in text.js before metadata extraction; preserve metadata from RAW source first. Imported annotations and detected dots must point to real occurrences.
5. Source object imports currently preserve only joined verse text as original/hash, losing structured refs/metadata; either reject object form and require exact raw JSON string or retain canonical complete object plus original transport. The Qere case already reported includes both alternatives with no specific warning. Do not certify bracketed study text.
6. onProgress callbacks inside a synchronous loop do NOT yield the event loop. Run full ~305k-letter workload with simultaneous health request/UI interactions, and implement a worker or actual yielding chunks. UI progress must reflect actual progress, not a timed animation.
7. Review the prior Shem coverage report (יה, אהיה, אלוה, suffixed forms) and compare locked snapshot behavior at the HTTP boundary, not only helper tests. Line IDs derived solely from amud/line numbers must NEVER cause progress to transfer to different text after candidate adoption.

Do not stop at passing self-authored narrow tests. Fix material cases through the requested Fireworks DeepSeek model, then have Opus5 review the actual resulting tree. The controller has not changed product code.

Executed evidence (controller read-only probes):
- Sheet geometry independent fixtures PASS: four100mm columns,3per sheet,5mm inter-column gaps,3mm outer margins => exact final sheet0.422m; rounded0.632m.
- Replacing same1mm stretch twice changes leftover94.5→93.5→92.5, proving non-idempotence.
- Duplicate1mm stretches for one occurrence with1.5mm cap both accepted, warnings[].
- `אב {ס} גד` produces items `[אב, גד, setuma_gap]`, proving boundary relocation.
- Raw `א\u05c4ב` produces unusual_letters[].
- max_inter_word_gap_mm99 against small letter1.2mm produces no spacing-bound errors. Enforce configured maximum strictly below calibrated small-letter width, even if the current word gap itself is smaller.
- HTTP origin helper permits Origin:null and arbitrary localhost ports. Validate full same-origin scheme/host/port; token is additional protection, not permission to ignore the contract.
- getLayoutLines currently persists no words/items/Shem-token metadata/letter-level measures, so verify preview, stretch, fixed segments and unusual overrides after server restart, not just immediately after compute. Do not reconstruct against a changed live profile/source.
