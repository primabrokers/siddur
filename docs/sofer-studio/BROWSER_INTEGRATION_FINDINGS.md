Controller started a PRIVATE preview using the actual backend on127.0.0.1:4247 and separate ignored data/preview.db. Remote process currently1993433 (verify before restarting), log/tmp/sofer-studio-preview.log. Local SSH tunnel exposes same port. Health200; real frontend loads, no startup JS errors. Do not wipe preview.db; it contains controller-labelled QA fixtures only. No public deployment.

Browser QA at1280×720 after reload of current assets:
- First-run notice works; source/profile/geometry save, pasted-source import and Compute execute through real HTTP. QA excerpt is `בראשית ברא אלהים את השמים ואת הארץ\nוהארץ היתה תהו ובהו וחשך על פני תהום`.
- Calibration panel badly overflows horizontally: second column of controls clipped. Action buttons stack into a tall column beside profile name. Profile-name input and bottom tabs have white backgrounds with nearly white text, unreadable. Fix CSS grid min-width, panel breakpoints, flex wrapping and form/button tokens; test real desktop/phone screenshots, not only syntax. Prior reference-QA explicitly warned of this.
- SERIOUS numerical mismatch: calibration.js calls letter_widths skeleton mm and shows Total א2.10mm for defaultwidth2.4/ref6/H4.5/stroke.3. Backend contract stores letter_widths in UNITS with unit_mm.5 => engine total1.20mm. One canonical schema and measurement path; editable units/mm as requested. max_stretch is backend absolute mm but UI labels caps×. Reconcile units and enforce same cap in UI/server. Unit setting is not visible despite brief.
- Geometry panel reports3×למשפחותיכם=9.00mm with saved QA profile and130mm column, incorrectly green. It was0mm before saving a profile, also green. Likely using listProfiles summaries without letter_widths/gaps/reference height instead of hydrated profile. Never show passed guard from missing data. Derive threshold from same full calibrated profile as backend, including2word gaps.
- Compute status says layout UUID/1amud but header still saysNO LAYOUT and dropdowns remain—none—until reload. Normalize compute/layout IDs and update shared selection/state consistently.
- Text line runs out of parchment horizontally in computed tikkun. Preserve measured per-letter boxes/gaps and use explicit fitting/scroll scale; do not clip text. Tikkun must match same measured layout used for export, including after saved-layout reopen/restart.
- Protected-names panel lists אלהים underCertain, while backend marks ambiguous elohim uncertain. Per-token classification is being lost/recomputed in frontend. Preserve canonical uncertainty and actual source occurrence metadata.
- Pattern UI claims 'normal wrapping NOT applied' merely because data missing, but backend currently wraps those passages when no patterns selected. Claims must reflect real blockers and behavior, never a decorative status label.

Backend child finished; frontend may still be polishing. Recheck current tree, fix material integration through Fireworks DeepSeek, and have Opus5 review actual code/tests. Controller has not edited product files. Full-size pure-engine probe on fetched study corpus:305172letters/5846verses, source586ms, layout1674ms,6798lines,163MBheap. This is performance ONLY, not certified layout.
