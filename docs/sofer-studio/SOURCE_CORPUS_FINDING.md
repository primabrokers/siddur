Controller obtained actual pinned raw Sefaria v3 responses for all five books, available now under docs/sofer-studio/source-data/{Genesis,Exodus,Leviticus,Numbers,Deuteronomy}.json plus manifest.json with exact URLs, hashes, Public Domain license and version provenance. No paid/new provider involved. Requested explicit full chapter ranges, version hebrew|Tanach with Text Only, fill_in_missing_segments=0, verified expected chapter counts and zero warnings. Total 5,846 verses (source's numbering); use actual counts, not hard-coded canonical total.

CRITICAL IMPORT ISSUE: this source includes KETIV plus bracketed QERE, e.g. Genesis8:17 `הוצא [היצא]`, Exodus4:2 `מזה [מה ] [זה]`. Removing punctuation and including both is WRONG for written Torah. Raw Hebrew-letter count is305172 because of alternative readings and possible variants; do not claim304805 or certification. Preserve raw originals. A preview-only explicit ketiv policy could drop bracketed qere with recorded normalization, but unresolved variants, absent complete paragraph/unusual-letter/Shem/pattern metadata must remain visible blockers for writing/export readiness. Do not silently normalize ambiguous content or guess correct readings. A complete study text is not a complete certified scribal source.

These files are available for built-in Chumash import and realistic full-corpus performance coverage, and to exercise qere/ketiv ambiguity warnings. Integration should be done by DeepSeek after current implementation children finish if they cannot receive this note live. Do not fabricate missing authoritative layout annotations.

Backend deps already installed by controller (npm install --no-audit --no-fund inside sofer-studio, produced package-lock.json). Existing root app untouched.
